# FORM_EQUIPMENT — Equipment and Systems Request

> Generated by `review/tools/render-fields.js` from `InitialRequest.extracted.json` +
> `InitialRequest.notes.json`. Do not hand-edit this file — edit the notes and re-render.
> Descriptive only. It records what the form does, never whether that is correct or
> intended — that is the maintainer's call, made during review of each flow.

| | |
|---|---|
| **Route** | `?form=equipment_request` (`ROUTE_EQUIPMENT_REQUEST`) |
| **Template** | `InitialRequest.html` |
| **Mode** | `equipment` (baseMode `equipment`) |
| **Fields visible** | 43 of 52 defined |
| **Required here** | 9 |
| **Submit contract** | Explicit map — the page builds a named object, so **only listed controls reach the server**. A new control is not submitted until it is added to that object. |

> **Shared template.** `InitialRequest.html` also serves `FORM_NEW_HIRE`, `FORM_IT_CONFIRMATION`. One template, one
> definition per field. The maintainer states this sharing is intended, so a field is only
> edited once. Each mode is still a
> distinct form in use, with its own route, audience and visible/required set. **An edit to
> any field below lands on every mode listed here.**

## Used as a step in

A form is a reusable step component. The same form in two workflows is two step
instances — different entry condition, actor and effects — and each is reviewed
separately as part of its own flow.

| Step instance | Workflow | # | Actor | Entry condition | Sets step to |
|---|---|:--:|---|---|---|
| `SI_EQUIPMENT_1_SUBMIT` | System & Equipment Request | 1 | ACTOR_REQUESTER | always — creates the workflow | IT Confirmation Needed |

## Fields

### Requester Information

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| Date Requested * | `dateRequested` | date | ● | always | Stored as Date Requested; shown in the dashboard Date Requested column. |
| Requester Email * | `requesterEmail` | email | ● | always | Becomes the workflow initiator. Grants that person ROLE_REQUESTER (tier 6) — cancel and remind rights on this request. Receives every status email. |
| Requester Name * | `requesterName` | text | ● | always | Stored; shown as Requester on the dashboard and in the step tracker. |

### Employee Information

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| First Name * | `firstName` | text | ● | always | Half the employee name used for the workflow record, emails, task titles and the duplicate-submission key. Also seeds the DSS username as first.last at ID Setup. |
| Middle Name | `middleName` | text |  | always | Stored only. |
| Last Name * | `lastName` | text | ● | always | As firstName — name, dedupe key and DSS username seed. |
| Preferred Name | `preferredName` | text |  | always | Stored only. |
| Job Title * | `position` | text | ● | always | Stored as Position Title; appears in specialist task descriptions and emails. Changing it at HR Verification notifies Safety and ID Setup. |
| Site Name * | `siteName` | select | ● | always | Stored as Site Name; shown in the dashboard Site column and carried into Safety and ID Setup context. Changing it at HR Verification notifies Safety. |
| Department (Optional) | `department` | text |  | always | Stored; carried into the workflow context block on emails. |
| Reporting Manager Email * | `managerEmail` | email | ● | always | Becomes the assignee of the WIS Assignment task and grants that person ROLE_MANAGER (tier 7). Shown on the dashboard and used for the My Open Requests panel. |
| Reporting Manager Name * | `managerName` | text | ● | always | Stored; shown in emails and the request header. |

### Systems/Apps

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| Systems / Apps <br><sub>`ADP Supervisor Access` · `BOSS` · `CAA` · `Delivery` · `Fleetio` · `Google Account` · `Incidents` · `Central Purchasing/Jonas` · `Net Promoter Score` · `SiteDocs Supervisor`</sub> | `systems` | checkbox |  | `#systemAccessSection` is open (`toggleSystemAccess`) | CREATES TASKS. 'BOSS' inserts the IT Confirmation step before IT Setup. 'Fleetio' creates the Fleet task. 'SiteDocs' creates the ID Setup task on equipment requests only — on a new hire the SiteDocs account is made during ID Setup. 'Google Account' reveals the Google account fields. Stored as a comma-separated list. |
| ADP Salary Access | `adpSalaryAccess` | checkbox |  | `#adpSitesSection` is open (`toggleSections`) | When Yes, the Payroll notification sent after HR Verification carries an explicit salary-access callout. |
| BOSS Training User Only <br><sub>`Yes` · `No`</sub> | `bossTrainingOnly` | radio |  | `#bossDetailsSection` is open (`toggleSections`) | Changes the WIS task wording to training modules only, explicitly excluding committee, cost sheet, trip reports and grievances. |
| BOSS Cost Sheet <br><sub>`Yes` · `No`</sub> | `bossCostSheet` | radio |  | `#bossDetailsSection` is open (`toggleSections`) | Stored as a BOSS detail; becomes an IT checklist line. Reveals the cost sheet job numbers picker when Yes. |
| BOSS Trip Reports <br><sub>`Yes` · `No`</sub> | `bossTripReports` | radio |  | `#bossDetailsSection` is open (`toggleSections`) | Stored as a BOSS detail; becomes an IT checklist line. |
| BOSS Grievances <br><sub>`Yes` · `No`</sub> | `bossGrievance` | radio |  | `#bossDetailsSection` is open (`toggleSections`) | Stored as BOSS Grievances; becomes an IT checklist line. |
| Requested Email Address | `googleEmailLocal` | text |  | `#googleAccountDetailsSection` is open (`toggleSections`) | The requested mailbox name. Combined with the domain it seeds the SiteDocs username default at ID Setup, and IT confirms or changes it at IT Setup. |
| Email Domain | `googleDomain` | select |  | `#googleAccountDetailsSection` is open (`toggleSections`) | The requested domain, combined with the local part above. |

### Equipment

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| Equipment <br><sub>`Business Cards` · `Computer` · `Credit Card` · `Mobile Phone` · `SiteDocs Tablet` · `Vehicle`</sub> | `equipment` | checkbox |  | `#systemAccessSection` is open (`toggleSystemAccess`) | CREATES TASKS. 'Business Cards' creates the Business Cards task. 'SiteDocs Tablet' counts toward the SiteDocs indicator on the dashboard. Everything else becomes IT provisioning checklist lines. Stored as a comma-separated list. |

### Computer Details

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| Computer Request Type <br><sub>`New` · `Reassignment`</sub> | `computerRequestType` | radio |  | `#computerDetailsSection` is open (`toggleEquipmentDetails`) | New or reassigned. Chooses which computer detail block is shown, and becomes an IT checklist line. |
| Computer Type <br><sub>`Chromebook` · `Windows` · `Mac`</sub> | `computerType` | radio |  | `#computerNewFields` is open (`toggleComputerReassignFields`) | Stored; IT checklist detail for a new machine. |
| Office 365 Required <br><sub>`Yes` · `No`</sub> | `office365Required` | radio |  | `#office365Question` is open (`toggleOffice365Question`) | Stored; IT checklist detail. |
| Previous User Name | `computerPreviousUser` | text |  | `#computerDetailsSection` is open (`toggleEquipmentDetails`) | Stored; IT checklist detail for a reassigned machine. |
| Previous Computer Type | `computerPreviousType` | select |  | `#computerDetailsSection` is open (`toggleEquipmentDetails`) | Stored; IT checklist detail for a reassigned machine. |
| Serial Number (if known) | `computerSerialNumber` | text |  | `#computerDetailsSection` is open (`toggleEquipmentDetails`) | Stored; IT checklist detail for a reassigned machine. |

### Credit Card Details

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| USA Credit Card | `creditCardUSA` | checkbox |  | `#creditCardDetailsSection` is open (`toggleEquipmentDetails`) | CREATES A TASK. Any of the three card boxes creates the Finance task; this one adds the USA card line with its requested limit. |
| USA Credit Card | `creditCardLimitUSA` | text |  | `#creditCardDetailsSection` is open (`toggleEquipmentDetails`) | Requested monthly limit, quoted on the USA line of the Finance task. Falls back to 'Standard' when blank. |
| Canada Credit Card | `creditCardCanada` | checkbox |  | `#creditCardDetailsSection` is open (`toggleEquipmentDetails`) | CREATES A TASK. Adds the Canada card line to the Finance task. |
| Canada Credit Card | `creditCardLimitCanada` | text |  | `#creditCardDetailsSection` is open (`toggleEquipmentDetails`) | Requested monthly limit for the Canada card; 'Standard' when blank. |
| Home Depot Credit Card | `creditCardHomeDepot` | checkbox |  | `#creditCardDetailsSection` is open (`toggleEquipmentDetails`) | CREATES A TASK. Adds the Home Depot card line to the Finance task. |
| Home Depot Credit Card | `creditCardLimitHomeDepot` | text |  | `#creditCardDetailsSection` is open (`toggleEquipmentDetails`) | Requested monthly limit for the Home Depot card; 'Standard' when blank. |

### Mobile Phone Details

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| Mobile Phone Request Type <br><sub>`New` · `Reassignment`</sub> | `phoneRequestType` | radio |  | `#mobilePhoneDetailsSection` is open (`toggleEquipmentDetails`) | New or reassigned. Chooses which phone detail block is shown; becomes an IT checklist line. The number is also reused on termination to build the phone suspension line. |
| Previous User Name | `phonePreviousUser` | text |  | `#phoneReassignFields` is open (`toggleComputerReassignFields`) | Stored; IT checklist detail for a reassigned phone. |
| Previous Phone Number | `phonePreviousNumber` | tel |  | `#phoneReassignFields` is open (`toggleComputerReassignFields`) | Stored; IT checklist detail for a reassigned phone. |

### Additional Comments

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| Comments | `comments` | textarea |  | `#systemAccessSection` is open (`toggleSystemAccess`) | Stored as Comments; carried into the context block on downstream emails. |
| Review Notes (Optional) | `notes` | textarea |  | always | Present on the page but not included in the submitted payload. |

### (no section — built at runtime)

| Label | Control | Type | Req | Shown when | Effect |
|---|---|---|:--:|---|---|
| BOSS Job Sites | `bossJobSites` | checkbox *(built at load)* |  | `#bossCommitteesCheckboxes` is open | Built at load from reference data. Stored as the BOSS job sites list. |
| BOSS Cost Sheet Job Numbers | `costSheetJobNumbers` | checkbox *(built at load)* |  | `#costSheetJobCheckboxes` is open | Built at load from reference data. Stored as the BOSS cost sheet job list; only meaningful when Cost Sheet is Yes. |
| ADP Sites | `adpSites` | checkbox *(built at load)* |  | `#adpSitesCheckboxes` is open | Built at load from reference data. Stored as the ADP sites list; revealed when ADP is among the systems. |
| Central Purchasing Sites | `purchasingSites` | checkbox *(built at load)* |  | `#purchasingSitesCheckboxes` is open | CREATES A TASK. Built at load from reference data. Any value creates the Purchasing task (combined with Jonas job numbers if both are set), with one line per site. |
| Jonas Job Numbers | `jonasJobNumbers` | checkbox *(built at load)* |  | `#jonasJobNumbersCheckboxes` is open | CREATES A TASK. Built at load from reference data. Any value creates the Purchasing task, with one checklist line per job number. Stored comma-separated. |

## Not shown in this mode

| Control | Why |
|---|---|
| `hireDate` | .new-hire-only is hidden in equipment mode (via .form-row.new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `hireType` | .new-hire-only is hidden in equipment mode (via .form-row.new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `employeeType` | .new-hire-only is hidden in equipment mode (via .form-row.new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `employmentType` | .new-hire-only is hidden in equipment mode (via .form-row.new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `jobSiteNumber` | explicitly disabled, un-required and its wrapper hidden in equipment mode |
| `systemAccess` | .new-hire-only is hidden in equipment mode (via .question-wrapper.new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `jrRequired` | .new-hire-only is hidden in equipment mode (via .new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `jrAssignment` | .new-hire-only is hidden in equipment mode (via .new-hire-only); .new-hire-only controls are disabled in equipment mode |
| `plan306090` | .new-hire-only is hidden in equipment mode (via .new-hire-only); .new-hire-only controls are disabled in equipment mode |

## Visible but not submitted

Present on the page, not included in the payload the handler receives.

- `notes` — Review Notes (Optional)

## Built at load time

These have no markup — the page renders them from reference data, so they are invisible to
a markup-only read of the template.

| Control | Container | Built by | From |
|---|---|---|---|
| `bossJobSites` | `#bossCommitteesCheckboxes` | `buildDualList` | `INJECTED_REF_DATA.committees` |
| `costSheetJobNumbers` | `#costSheetJobCheckboxes` | `buildDualList` | `INJECTED_REF_DATA.jobs` |
| `adpSites` | `#adpSitesCheckboxes` | `buildDualList` | `INJECTED_REF_DATA.jobs` |
| `purchasingSites` | `#purchasingSitesCheckboxes` | `buildDualList` | `INJECTED_REF_DATA.jobs` |
| `jonasJobNumbers` | `#jonasJobNumbersCheckboxes` | `buildDualList` | `INJECTED_REF_DATA.jobs` |

---

_Effects documented: 43/43. Snapshot 2026-09-19._
