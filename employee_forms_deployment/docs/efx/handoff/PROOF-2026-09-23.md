# Proof — 2026-09-23, production

Everything here was run against **production** on 2026-09-23 (ET), not a test tier. Nothing simulated.

## Apps Script

| Check | Result |
|---|---|
| Prod sheet EFX migration (`migrateEfxDryRun` on `1kGjw8e…`) | `'Internal Employee ID' header already present`, `'Employee IDs' sheet already present`, `Raw Log already has 'Event ID'` — all no-op |
| Local suites before push | super-test 159/0 · form-field-map 315/0 · efx-test 218/0 · migration 42/0 · efx-e2e 596 passed, 0 real failures, 3 recorded defects |
| `clasp push -f` to `1AuIbJl1jR…` | 86 files |
| `clasp deploy -i AKfycbyXp4q0…` | `@78` — "EFX go-live 2026-09-23: n8n alias layer + Internal ID in submit emails" |
| Version 78 content (Apps Script API `projects.getContent`) | `EmailUtils` has `preassignedEmployeeId`; `EmailTemplates` has the `Internal ID` row; `N8N_API_VERSION = 2026.09.17-1` |
| Deployments after | `AKfycbyXp4q0…` **v78** DOMAIN (portal) · `AKfycbwAb33…` v75 DOMAIN (stray) · `AKfycbw7fhvIk…` v76 ANYONE_ANONYMOUS (George's doPost, untouched) · HEAD |
| Commit | `edf6d0c` on `main`, pushed |

## Router door (`EFX · Test Trigger (PROD)` → `EFX · Router (PROD)` → Execution API)

```json
n8n_ping → {"ok":true,"principal":"efx-bot@team-group.com","env":"PROD","apiVersion":"2026.09.17-1",
            "spreadsheetId":"1kGjw8e-uIehaBemlsRZ4Yq1QrYOWkJvWzhKbgfl4Pxo","requestId":"REQ-3F4A1550"}
n8n_listWorkflows({type:'NEW_EMP',limit:3}) → total 558, source Dashboard_View
n8n_getWorkflow('NEW_EMP_20260120-113623_780') → requestData (57 headers), checklist[5], employeeId{…}
n8n_listTasks({status:'Open'}) → count 487
```

## Wrappers created today (all `callerPolicy: any`, Router `ZOFcqwdAjXNHfsbH`, active)

| Wrapper | id |
|---|---|
| `Forms · Get Workflow (PROD)` | `A7nqVbqn19kzaSfi` |
| `Forms · List Tasks (PROD)` | `5HoAXNxCWVF6wtYx` |
| `Forms · Close Task (PROD)` | `C4oJSaevEqMvSCZe` |

Proven by a temporary caller workflow (since deleted) invoking them **as sub-workflows** against a live hire
with an open task:

```json
{"summary":{"getOk":true,"requestDataKeys":57,"listOk":true,"taskCount":1,
            "pickTaskId":"TK-8CCA4E93","pickFormType":"safety_onboarding"},
 "closeDryRun":{"ok":true,"dryRun":true,"wouldClose":true,"taskId":"TK-8CCA4E93",
                "workflowId":"NEW_EMP_20260512-084806_165","fn":"n8n_closeTask","requestId":"REQ-2A9AFA58"}}
```

The dry run wrote nothing; the task is still Open.

### Defect found and fixed on the way

The first caller used Execute Workflow **typeVersion 1.1**. It ignored the field mapping and passed the
previous node's fields by name, so List Tasks received `status:"Complete"` and returned 0. At **1.2**, with a
Code node shaping the exact inputs first, it returned the task. Every demo and the template use 1.2 + shaping.
Recorded in `HANDOFF.md` §3 and `AGENT.md`.

## Workspace

`grp.forms.idsetup@team-group.com` members after `gam update group … add member`:
dan.anger, **dbinns@robinsonsolutions.com**, **george.anthony@team-group.com**, jennifer.myers, pamelabarber,
reannareisch.

## Demo workflows (all created **inactive**)

| Workflow | id | Project |
|---|---|---|
| `DEMO · ID Setup from submit email (Binns' inbox) (PROD)` | `52DqtvlqJgO0ABL5` | Daniel Binns |
| `DEMO · ID Setup from submit email (George's inbox) (PROD)` | `etvSuKXluKGrNQAT` | Team Group (transferred, HTTP 204) |
| `DEMO · Close JR Task via EFX (PROD)` | `KDlp3JYyY2Q170gX` | Daniel Binns |

## Not yet proven today

- A real `ID Setup Required` email rendered by v78 (the test suite proves the HTML; the first live submit on
  the call will show it).
- An end-to-end run of the demo workflow (email → approve → Submit ID Setup) on production. Its parts are
  proven: the email content (tests), Get Workflow (above), Submit ID Setup (PROD) is byte-identical in logic to
  the TEST wrapper proven 2026-09-18, and the Wait/approve pattern ran 9/9 on 2026-09-18 (`PROOF-2026-09-18.md`).
- (resolved) all three new wrappers now sit in the *Employee Forms* folder with the other PROD wrappers.
