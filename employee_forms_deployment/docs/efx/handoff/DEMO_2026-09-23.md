# Demo script — 2026-09-23, live with George

*Step-by-step for the call. Everything below runs against PRODUCTION. The fake hire is deleted afterwards.*

## Before the call (Binns)

- [ ] Prod portal is on v78 (HEAD): `clasp list-deployments` shows `AKfycbyXp4q0… @78`. Done 2026-09-23 16:20 ET.
- [ ] n8n: `DEMO · ID Setup from submit email (Binns' inbox) (PROD)` — id `52DqtvlqJgO0ABL5` — **switch ON** at the start of the call. It polls Gmail every minute.
- [ ] n8n: `DEMO · ID Setup from submit email (George's inbox) (PROD)` — id `etvSuKXluKGrNQAT` — stays **OFF** until George says yes on the call (it reads his mailbox).
- [ ] n8n: `DEMO · Close JR Task via EFX (PROD)` — id `KDlp3JYyY2Q170gX` — manual, no switch needed.
- [ ] Heads-up to the ID Setup team: a **Demo Hire** submission will land in grp.forms.idsetup and, after ID Setup, an HR Verification email will go to HR. Ignore both.

## Part 1 — ID Setup, George's flow (10 min)

1. **Binns submits the Initial Request in the prod portal.** Hourly · Direct Hire · New Hire · System Access **No**. Name `Demo Hire <today>`. Requester = you.
   *Say:* the Internal Employee ID is minted **here**, at submit, and written to the sheet. Nothing waits for the ID Setup page.
2. **Open the two emails** that arrive within seconds:
   - `Request Submitted` → your inbox (requester)
   - `ID Setup Required` → grp.forms.idsetup → George's inbox and dbinns@robinsonsolutions.com
   Both show **Request ID** and **Internal ID** in Request Details. The ID Setup section reads *In Progress*, not complete.
   *Say:* this email is George's trigger. Adding him to the group was the whole integration — no poller, no webhook.
3. **Within a minute** the demo workflow fires. Show the execution: `Parse submit email` → `Forms · Get Workflow (PROD)` → `Draft accounts` → `Email approver`.
   *Say:* Get Workflow returns the full request plus the `employeeId` block. The email already had the id; the lookup is belt and braces and gives him everything else (site, position, systems).
4. **George opens the approval email** (to him, cc you): drafted SiteDocs worker id, job code, DSS username, one green button.
   *Say:* this is his step 3. `Draft accounts` is where his real SiteDocs / DSS / BOSS calls go. The approval gate is his to shape: reject, reminder, timeout.
5. **George clicks Approve.** The workflow resumes: `Build ID Setup` → `Forms · Submit ID Setup (PROD)` → `Confirm submitted`.
   *Show in the portal:* the hire is now at **HR Verification Needed**; the **Safety Onboarding** action item exists (hourly, no access). The ID Setup Results row says who submitted (the actor).
   *Say:* Forms did the sheet writes and the emails. n8n never touched a spreadsheet.
6. **Close the loop:** the `Confirm submitted` email to George shows Forms' reply and request id.

## Part 2 — JR, the one-node swap (5 min)

7. Open `DEMO · Close JR Task via EFX (PROD)`. Set `idOrWorkflow` to the demo hire's `NEW_EMP_…` id and run.
   Expected: **E_NOT_FOUND** — no JR task exists yet (JR tasks are created at IT Setup). The error email arrives with the `[efxCode=E_NOT_FOUND]` tag.
   *Say:* that is the error contract. In his real run the id is the `TK-…` from his tracker and the task closes with his actor recorded.
8. Show `docs/efx/handoff/snippet_MarkPortalJrComplete_PROD.json`: the exact node he pastes over `Mark Portal JR Complete` in `BOSS JR Assignment`. Target `zxaTLTTNK28LpnNg`. Mapping is already his expressions.

## Part 3 — Test as George (if he agrees, 3 min)

9. Switch **ON** `DEMO · ID Setup from submit email (George's inbox) (PROD)` (Team Group project, his Gmail credential). Switch **OFF** the Binns' inbox copy so the hire is not processed twice.
10. Either Binns submits a second `Demo Hire` (same path, now triggered from George's mailbox), **or** leave his copy ON and let the next real hire trigger it — George approves from his own inbox. Pick by the clock.

## Handover (2 min)

- Callable now, no allow-list needed (`callerPolicy: any`): `Forms · Get Workflow (PROD)` `A7nqVbqn19kzaSfi` · `Forms · List Tasks (PROD)` `5HoAXNxCWVF6wtYx` · `Forms · Close Task (PROD)` `C4oJSaevEqMvSCZe` · `Forms · Submit ID Setup (PROD)` `P3HiVnzoxaRYYTZN` · `Forms · Close JR Task (PROD)` `zxaTLTTNK28LpnNg`.
- His template: `docs/efx/handoff/template_IdSetup_from_submit_email_PROD.json` (the Binns' inbox demo, credentials stripped). Import, pick his Gmail credential, change the approver, replace `Draft accounts`.
- Docs: `README.md` (plain English), `HANDOFF.md` (human), `AGENT.md` (his agent), `FLOW.md` (pictures).

## After the call — cleanup

- Switch both ID Setup demos **OFF**.
- Cancel the demo hire(s) from the portal dashboard (Cancel Request). Forms cancels their open tasks.
- Optional: `cleanupSuperDebugWorkflow('<NEW_EMP_…>')` from the Apps Script editor removes the rows entirely.
- Share the five wrappers read-only with George's project when convenient (UI: workflow → Share). Calling by id already works.
