# Known defects found while mapping (pre-existing in prod unless marked)

Source: field-by-field mapping pass over the fork (`docs/mapping/*.md`, 2026-09-16). These exist in **prod** today. Only #9 and #11 were touched in the fork because they intersect the EFX paths; the rest are recorded here for a separate Forms backlog — **not fixed tonight** (out of scope for the TEST delivery; each needs its own review).

| # | Area | Defect | Where | Status in fork |
|---|---|---|---|---|
| 1 | IT Confirmation | Write-back reads `formData.hireType` but the client posts `newHireOrRehire` → "New Hire/Rehire" blanked on confirmation | `ITConfirmationHandler.js:70`, duplicate in `BOSSReviewHandler.js` | open (prod) |
| 2 | Emails | `getWorkflowContext` BOSS header lookups (`'BOSS Job Sites'`, `'BOSS Cost Sheet Access'`, `'BOSS Cost Sheet Jobs'`, `'BOSS Trip Reports'`) don't match sheet headers (`BOSS Sites`, `BOSS Cost Sheet`, `BOSS Jobs`, `BOSS Trip`) → always empty in email context | `EmailUtils.js` | open |
| 3 | Equipment | `wis_user` (SiteDocs account) task never created: condition requires `systems` value exactly `sitedocs`, checkbox value is `SiteDocs Supervisor` | `ITSetupHandler.js:576` | open |
| 4 | Status change | Approval routing tests `'Reporting Manager Change'`; checkbox value is `Manager Change` → dead branch | `PositionChangeHandler.js:426` | open |
| 5 | Status change | Checkbox arrays (`adpSalaryAccess`, `creditCard*`) stored raw via `\|\| ''` (array truthy) — works only via Sheets `toString()` | `PositionChangeHandler.js` | open |
| 6 | Requests | Workflow row created **before** validation in `submitEquipmentRequest` (`:41` vs `:49`), `submitTerminationRequest`, `submitPositionChangeRequest` → orphan `Workflows` rows on rejection | handlers | open (New Hire validates first — correct) |
| 7 | IT Setup (CHANGE_) | Double `IT Results` row: `submitITSetup` appends (`:312`) and `closeActionItem` special case 1 appends again | `ITSetupHandler.js`, `Services/ActionItemService.js:~250` | open |
| 8 | IT Confirmation | The `IT Confirmation` action item is never closed by any code path (non-blocking only because its category isn't in `getRequiredSpecialistCats`) | `HRVerificationHandler.js:287`, `ActionItemService.js` | open |
| **9** | Safety | With `SAFETY_TRAINING_AT_SUBMIT` on, HR Verification re-calls `sendSafetyOnboardingEmail` unconditionally → duplicate email / **second task** | `HRVerificationHandler.js:330` | **fixed in fork**: `sendSafetyOnboardingEmail` is now idempotent (skips if any Safety task exists for the workflow, open or closed) |
| 10 | Specialist legacy | Safety row writes 7 values under 6 headers; never closes the action item | `Specialist.js:92-100` | open (legacy path; `task.close` preferred) |
| **11** | Automation drafts | `jrCompleteViaSecret`/`completeMyTask` save a flat `{item:{…}}` draft; UI and `notifyTaskClosure` expect `{items:{…}}` → automation-closed tasks render all-Pending when reopened | `Services/ActionItemService.js:1054-1069, 1150-1158` | **fixed in fork for the EFX path**: `efxTaskClose` writes `{items:{…}}` and migrates legacy shapes. `jrCompleteViaSecret` left as-is (to be retired with @76). |
| 12 | ID Setup | `ID Setup Results` is append-only (re-submit → duplicate rows; readers disagree first vs last). `getITContextData:153` sets `idSubmittedBy` from the `BOSS WIS Created` column | `IDSetup.js`, `ITSetupHandler.js:153` | open |
| 13 | Validation | Formula-injection guard rejects **any** field beginning with `-`/`+`/`=`/`@` (e.g. a comment starting with a dash), not just formulas | `ValidationUtils.js:221-227` | open (documented in `FormContracts` notes so n8n payloads avoid it) |
| 14 | Minor | `autoFillJobNumber`/`populateSelect('jonasJobNumbers')` are no-ops; `adpSites`/`purchasingSites` show `*` but aren't enforced; `Equipment_Requests` sheet declared, never written | `InitialRequest.html:849,1129`, `Config.js:47` | open |

Also from the earlier review (`spec/01_…` §10): `submitSpecialistForm` sheet map dead → everything lands in `Specialist Results`; `submitITConfirmation` defined twice (prod vs dev differ); `generateEmployeeId` timestamp fallback (**removed in fork**).

Suggested handling: file #1–#8, #10, #12–#14 as a Forms backlog after the TEST deployment; none blocks the EFX test, but #3 and #8 affect what George can automate for Equipment and IT Confirmation.

## Added by the E2E suite (2026-09-16, `__tests__/efx-e2e-test.js` — asserted as `defect(...)`, reported not fatal)
| # | Area | Defect | Where | Status in fork |
|---|---|---|---|---|
| 15 | Finance task | Credit-card **requested limit never reaches the Finance checklist** — `getWorkflowContext` doesn't map Initial Requests cols 31/33/35, so `triggerSpecialists` always writes "Requested limit: Standard" | `EmailUtils.js` (context), `ITSetupHandler.js:483-485` | open (prod) |
| 16 | Status change | "Status Change Approved" routes `managerEmail` to the **old** manager — routing tests `'Reporting Manager Change'` but the UI/contract send `'Manager Change'` (same root as #4) | `PositionChangeHandler.js:426` | open |
| **18** | Workflow ids | `generateWorkflowId` = type + second-resolution timestamp + 3 random digits → two creates in the same second can **collide** and the second submit silently reuses the first workflow (seen as a 1-in-1000 flake in the E2E suite; realistic for n8n bursts) | `WorkflowManager.js:51-54,71` | **fixed in fork**: `createWorkflow` regenerates until the id is unused in `Workflows` (format unchanged) |
| **19** | Workflow ids | `createWorkflow` 30-second idempotency guard keys on (type, **initiator email**) only → under automation (constant requester) two *different* employees submitted within 30 s **merge into one workflow and one Internal Employee ID**; also true for one human submitting two hires quickly | `WorkflowManager.js:91-104`, `InitialRequestHandler.js:34` | **fixed in fork**: `createWorkflow(…, dedupeKey)`; Initial Requests dedupe on requester + employee + hire date (double-submit still deduped) |
| 20 | Position change | Requesting a **Vehicle** on a Position/Site Change creates **no task at all** — the IT task excludes Vehicle and there is no Fleet "add vehicle" task for `CHANGE_` (Fleetio access tasks exist, vehicle assignment does not) | `PositionChangeHandler.js` (task fan-out) | open (found by the end-user help pass 2026-09-16) |
| 21 | Safety | Hourly + no-system-access hires: the Safety Onboarding task is created at ID Setup and described as blocking, but that path marks the workflow **Complete at HR Verification** regardless → an Open Safety task on a Complete request | `HRVerificationHandler.js`, `ActionItemService` blocking rules | open (help pass) |
| 22 | ID Setup UI text | Hint "Auto-generated. Modify if needed or use existing ID for rehires." is stale: non-admins cannot change the pre-assigned id (fork), and the rehire-carry field (`existingInternalEmployeeId`) has **no UI control** — only automation can carry an id | `EmployeeIDSetup.html:40`, `InitialRequest.html` | open — UI text change deferred (no-UI-change rule for this delivery) |
| 23 | Access | `stgroup.ca` is accepted as a manager-email domain on the forms but is not in the portal's `ALLOWED_DOMAINS` → a manager at that domain receives links they cannot open | `ValidationUtils`/forms vs `Config.js ALLOWED_DOMAINS` | open (help pass) |
| 24 | Position/Site change | JR and 30/60/90 answers are collected and stored but **no task is ever created** for `CHANGE_` (no consumer) | `PositionChangeHandler.js`, mapping `OTHER_FORMS_DRAFT.md` | open (help pass) |
| 17 | Status change | Fleetio **add + remove collapse into one task** — `createActionItem` dedups on workflowId+category+formType and both are `Fleet/fleetio`; the removal checklist is dropped while both emails still send | `Services/ActionItemService.js:64-80`, `PositionChangeHandler.js:235,273` | open |

Oddities recorded by the same suite (not defects): prod handlers mutate the payload object they receive (workflowId/formId/timestamp) — **mitigated in fork**: `n8nSubmit_` hands handlers a copy, so an n8n retry with the same item is safe (the UI path is unchanged); the create `submit` Raw Log event has no workflowId (the `result` event does); `it_confirmation` expects arrays (CSV `purchasingSites` yields a spurious "Information Updated").
