# Kickoff for 2026-09-18 — paste everything below into a NEW session

---

Read these before doing anything, in this order:

1. `docs/runs/2026-09-17-SUMMARY.md` — yesterday in one page
2. `docs/runs/2026-09-17-efx-test-deploy-runlog.md` — the detail, with evidence
3. `docs/plans/DEPLOY_NOTES_FROM_TEST_2026-09-17.md` — what a dev/prod promotion needs
4. `docs/plans/CHECK_AND_DEPLOY_PLAN.md` — the gate model (Gates 0–6 are done)

Then tell me where things stand in your own words before touching anything, so I know you actually
read them rather than pattern-matched.

## How I want you to work

- **One gate or task at a time.** State the plan, wait for my approval, then run that one thing.
  Do not chain several together because they seem obviously next.
- **Short answers.** Bullets. I will ask if I want detail. Long responses are worse than useless.
- **When I have to do something myself**, tell me what, why it cannot be automated, and how, with a
  clickable link. One item at a time.
- **Verify, do not assume.** Yesterday cost real time to things that "looked obviously fine".
  If you have not seen the output, say so rather than implying it passed.
- There is a gate-verifier subagent at `.claude/agents/efx-gate-verifier.md`. Use it at gate
  boundaries. It has twice caught things I got wrong.

## State as of end of 2026-09-17

**Nothing is in production. Nothing is scheduled to fire. George's setup is untouched.**

| Thing | Where |
|---|---|
| TEST sheet | `1cjDM9uQy_tejnM0A11lrDLgBzQvncDGX44oTpE9hTvk` |
| TEST script | `1yD_Me_Y_zVZBoejOozy1CGVEFh2_dzw289SbVohVpWt2LEKXYMx_OYIn` |
| GCP project | `efx-test-teamgroup` (number `839437162862`) |
| Bot | `efx-bot@team-group.com`, delegation working |
| n8n project | Binns personal, `0wpVg3Nd6BJATZle` |
| n8n Router | `v9KymVILUsbb4zb5` — ACTIVE (sub-workflow only, runs only when called) |
| n8n test trigger | `OaGfiIi7l5o9NUs1` — **deactivated overnight**, reactivate to test |
| Canary / events poller | both OFF, deliberately |
| DEMO JR copy | `uQeizlMybFNGWj9N` — inactive |
| DEMO ID Setup gate | `qd3vlXvkAY8H2PC3` — inactive, **never run** |
| George's live workflow | `J7RU99n01pq9Xk3D` — ACTIVE, untouched, must stay that way |

Credentials live under `D:/Credentials/` and are referenced by path only. Test tooling is in the
previous session's scratchpad and will be gone; rebuild what you need.

## Today's plan

1. **Manually review the two demos.** I run them, you watch and explain. The ID Setup one has never
   executed — its approval form node is unproven, so expect to fix something.
2. **Full recap and document review.** The wiki and plans were written before yesterday and now
   contain things that are wrong. At minimum: error workflows must branch on the new `[efxCode=…]`
   tag, the Router flattens the response envelope, and `clasp create` needs `--type standalone`.
3. **Finish Gate 7 wrap.** Mainly: remove the service account's direct writer access on the TEST
   sheet. It was scaffolding so the migration would not have to wait for delegation.

## Decisions still open

- **Un-gate `it_confirmation`?** Its data-loss bug is fixed and proven, and the duplicate handler is
  gone, but it still does not close its own action item. Currently `verified:false`.
- **Which employee-number series continues**, 30k or 151k? They ran in parallel Mar–May. Needs
  somebody who knows the history, not a guess.
- **When does George see any of this?** Nothing goes to him until I say so.

## Traps that cost time yesterday

- `clasp create` **silently overwrites `appsscript.json`** with a bare template, destroying the
  scopes and the execution API config. Check `git status` immediately after.
- `clasp create --type webapp` fails on clasp 3. Use `--type standalone`.
- Granting an admin role or a Drive ACL is refused in auto mode regardless of permission rules.
  Either bypass mode, or run `migration/setup-tier-doors.ps1` yourself.
- The n8n public API has **no way to run a workflow**. Nothing in the EFX set is externally
  triggerable except the test webhook that exists for exactly that reason.
- n8n will not publish a workflow whose sub-workflow is unpublished.

Start by reading, then tell me where we are.
